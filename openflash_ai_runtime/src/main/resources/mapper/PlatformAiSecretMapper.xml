<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="openflash_ai_runtime.mapper.PlatformAiSecretMapper">
    <resultMap id="secret" type="openflash_ai_runtime.entity.PlatformAiSecret">
        <constructor>
            <idArg column="connection_id" javaType="_long"/>
            <arg column="secret_enc" javaType="java.lang.String"/>
        </constructor>
    </resultMap>

    <select id="findByConnectionId" resultMap="secret">
        SELECT connection_id, secret_enc
        FROM pw_platform_ai_secret
        WHERE connection_id = #{connectionId}
        LIMIT 1
    </select>

    <insert id="upsert">
        INSERT INTO pw_platform_ai_secret (connection_id, secret_enc)
        VALUES (#{connectionId}, #{secretEnc})
        ON CONFLICT (connection_id) DO UPDATE SET secret_enc = EXCLUDED.secret_enc
    </insert>
</mapper>
