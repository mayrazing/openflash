<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="openflash_plugin.tts.mapper.TtsDeckSettingsMapper">

    <resultMap id="TtsDeckSettingsResultMap" type="openflash_plugin.tts.entity.TtsDeckSettings">
        <constructor>
            <arg column="deck_id" javaType="java.lang.Long"/>
            <arg column="auto_speak_a" javaType="java.lang.Boolean"/>
            <arg column="auto_speak_b" javaType="java.lang.Boolean"/>
            <arg column="engine" javaType="java.lang.String"/>
        </constructor>
    </resultMap>

    <select id="findByDeckId" resultMap="TtsDeckSettingsResultMap">
        SELECT deck_id, auto_speak_a, auto_speak_b, engine
        FROM pw_tts_deck_settings
        WHERE deck_id = #{deckId}
        LIMIT 1
    </select>

    <insert id="insert">
        INSERT INTO pw_tts_deck_settings (
            deck_id, auto_speak_a, auto_speak_b, engine, updated_at
        ) VALUES (
            #{deckId}, #{autoSpeakA}, #{autoSpeakB}, #{engine}, NOW()
        )
    </insert>

    <update id="update">
        UPDATE pw_tts_deck_settings
        SET auto_speak_a = #{autoSpeakA},
            auto_speak_b = #{autoSpeakB},
            engine = #{engine},
            updated_at = NOW()
        WHERE deck_id = #{deckId}
    </update>

    <delete id="deleteByDeckId">
        DELETE FROM pw_tts_deck_settings
        WHERE deck_id = #{deckId}
    </delete>

</mapper>
